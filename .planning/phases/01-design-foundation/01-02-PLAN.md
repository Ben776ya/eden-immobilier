---
phase: 01-design-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: false
requirements:
  - TYP-02

must_haves:
  truths:
    - "The site builds and runs without errors after all Phase 1 config changes"
    - "Outfit font renders visibly in the browser (not system-ui fallback)"
    - "The typographic scale uses weight variation (300-600) rather than two font families"
  artifacts: []
  key_links:
    - from: "app/layout.tsx"
      to: "tailwind.config.ts"
      via: "CSS variable --font-outfit"
      pattern: "--font-outfit"
---

<objective>
Verify the design foundation is correctly wired: fonts load, tokens resolve, and the build is clean.

Purpose: TYP-02 requires a consistent typographic scale where headings feel family-related and body text is 16-18px. Since Phase 1 only changes config files (no component modifications), TYP-02 is verified by confirming the font system is active and the weight hierarchy is available. The visual checkpoint confirms the font variable chain works end-to-end (layout.tsx --font-outfit -> tailwind.config.ts fontFamily.sans -> rendered text).

Output: Verified working design foundation ready for Phase 2 component migration.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-design-foundation/01-RESEARCH.md
@.planning/phases/01-design-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build and verify font file generation</name>
  <!-- No files modified — this is a verification-only task that runs build/lint and inspects output -->
  <files></files>
  <read_first>
    - tailwind.config.ts (confirm token system from Plan 01)
    - app/layout.tsx (confirm Outfit import from Plan 01)
    - app/globals.css (confirm base layer updates from Plan 01)
  </read_first>
  <action>
1. Run `npm run build` — this must complete with zero errors. The build confirms:
   - TypeScript types are valid across all files
   - Tailwind processes the new config without errors
   - next/font/google downloads and self-hosts Outfit at build time

2. After the build completes, verify Outfit font files were generated:
   Run `ls .next/static/media/` and confirm at least one file with "Outfit" or a hash-based font file exists.

3. Run `npm run lint` — must pass with no violations.

4. Verify the font variable chain is intact by running these checks:
   - `grep "\-\-font-outfit" app/layout.tsx` — must return the variable declaration
   - `grep "\-\-font-outfit" tailwind.config.ts` — must return the fontFamily reference
   - These two must use the identical string `--font-outfit`

5. Verify no old font references leak through:
   - `grep -r "Playfair\|playfair\|DM_Sans\|dmSans\|font-serif" app/layout.tsx app/globals.css tailwind.config.ts` — must return 0 matches

6. Verify all 10 eden tokens exist:
   - `grep -c "ink:\|surface:\|card:\|gold:\|'gold-deep':\|'gold-light':\|text:\|'text-muted':\|white:\|border:" tailwind.config.ts` — must show at least 10 lines matched

TYP-02 verification: The typographic scale is established by the single-font system. Outfit at weights 300/400/500/600 provides the hierarchy documented in RESEARCH.md Pattern 4:
- 300 (light) for display headings
- 400 (regular) for body and section headings
- 500 (medium) for UI headings, labels
- 600 (semibold) for CTAs, nav links

The actual application of these weights to specific heading levels happens in Phase 2+ component work. Phase 1 establishes the AVAILABILITY of the scale.
  </action>
  <verify>
    <automated>cd "C:/Users/bench/OneDrive/Desktop/eden-immobilier" && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `npm run lint` exits with code 0
    - `.next/static/media/` directory contains font files (at least 1 file)
    - `grep -r "Playfair\|DM_Sans\|font-serif" app/layout.tsx app/globals.css tailwind.config.ts` returns 0 matches
    - `grep "\-\-font-outfit" app/layout.tsx` returns 1 match
    - `grep "\-\-font-outfit" tailwind.config.ts` returns 1 match
  </acceptance_criteria>
  <done>Build and lint pass. Outfit font files generated in .next/static/media/. Font variable chain --font-outfit is consistent between layout.tsx and tailwind.config.ts. No legacy font references remain in Phase 1 files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — Outfit font renders correctly</name>
  <what-built>
    Complete design foundation replacement:
    - New color token system (10 eden.* tokens replacing 9 old tokens)
    - Outfit font replacing Playfair Display + DM Sans
    - Updated globals.css base layer (no serif headings, token-based focus ring)
    - tailwindcss-motion plugin installed for future animation work
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. EXPECTED: Visual regressions WILL be visible — many components reference old token names (eden-bg, eden-cream, etc.) that no longer exist. Text may appear as default black on white, borders may disappear, backgrounds may be transparent. THIS IS EXPECTED and will be fixed in Phase 2.
    3. VERIFY THE FONT: Look at any visible text on the page. Outfit has distinctive rounded lowercase 'a' and 'g' letterforms. Compare against:
       - San Francisco (macOS system font) has a double-story 'a'
       - Segoe UI (Windows system font) has a narrower 'a'
       - If text looks like your system font, the font variable chain is broken
    4. Open browser DevTools > Elements > select the body element > Computed tab > look for "font-family". It should show "Outfit" (not system-ui or sans-serif).
    5. Open DevTools > Network tab > filter by "Font" > reload page. Outfit font files should load from the local server (not fonts.googleapis.com).
  </how-to-verify>
  <resume-signal>Type "approved" if Outfit renders correctly, or describe what you see if something looks wrong</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. `npm run build` passes — config files are valid
2. `npm run lint` passes — no code quality issues
3. Human confirms Outfit renders in the browser (not system font fallback)
4. All 10 eden.* color tokens are defined in tailwind.config.ts
5. No legacy tokens (bg, cream, burgundy, muted, border-light) remain in the 3 Phase 1 files
6. tailwindcss-motion is installed and registered
</verification>

<success_criteria>
- Build and lint pass without errors
- Outfit font confirmed rendering in browser via visual inspection
- Font variable --font-outfit chain is intact between layout.tsx and tailwind.config.ts
- Phase 1 config files contain zero references to old token names or old fonts
- The design foundation is ready for Phase 2 component migration
- NOTE: TYP-02 weight-application to specific heading levels (h1=300, h2=400, h3=500, etc.) is deferred to Phase 2 component migration. Phase 1 establishes the font availability and weight loading only.
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-foundation/01-02-SUMMARY.md`
</output>
